<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cocktail Recipe</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <% if (cocktail) { %>
            <h1><%= cocktail.strDrink %></h1>
            <img src="<%= cocktail.strDrinkThumb %>" alt="<%= cocktail.strDrink %>" width="300">
            <h3>Ingredients:</h3>
            <ul>
                <% for (let i = 1; i <= 15; i++) { %>
                    <% if (cocktail[`strIngredient${i}`]) { %>
                        <li><%= cocktail[`strIngredient${i}`] %> - <%= cocktail[`strMeasure${i}`] || '' %></li>
                    <% } %>
                <% } %>
            </ul>
            <h3>Instructions:</h3>
            <p><%= cocktail.strInstructions %></p>
        <% } else { %>
            <h1>Sorry, no cocktail found. Try again!</h1>
        <% } %>
        <a href="/">Get Another Cocktail</a>
    </div>
</body>
</html>
