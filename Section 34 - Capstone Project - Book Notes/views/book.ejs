<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add or Edit Book</title>
</head>
<body>
    <h1><%= book ? 'Edit Book' : 'Add New Book' %></h1>
    <form action="/books/edit/<%= book.id %>" method="POST">
        <label for="title">Title:</label>
        <input type="text" name="title" value="<%= book ? book.title : '' %>" required>
    
        <label for="author">Author:</label>
        <input type="text" name="author" value="<%= book ? book.author : '' %>" required>
    
        <label for="rating">Rating:</label>
        <input type="number" name="rating" min="1" max="5" value="<%= book ? book.rating : '' %>" required>
    
        <label for="date_read">Date Read:</label>
        <input type="date" name="date_read" value="<%= book ? book.date_read : '' %>">

        <label for="notes">Notes:</label>
        <textarea name="notes"><%= book ? book.notes : '' %></textarea>
    
        <button type="submit"><%= book ? 'Update Book' : 'Add Book' %></button>
    </form>
    
</body>
</html>
